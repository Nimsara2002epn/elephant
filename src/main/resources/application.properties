# ==========================================
# Elephant – Application Configuration
# ==========================================

# Server
server.port=8080

# ==========================================
# Database – MySQL
# ==========================================
spring.datasource.url=jdbc:mysql://localhost:3306/elephant_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=0513
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ==========================================
# Local profile — loads application-local.properties (git-ignored, contains JWT secret)
# ==========================================
spring.profiles.include=local

# ==========================================
# JWT — secret loaded from application-local.properties or JWT_SECRET env var
# Do NOT put the real secret here (this file is committed to Git)
# ==========================================
jwt.secret=${JWT_SECRET:}
jwt.expiration=86400000

# ==========================================
# Mail (Gmail SMTP – fill in your credentials)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=elephantreminder26@gmail.com
spring.mail.password=awms kquo uxvd xagt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
# File Upload
# ==========================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ==========================================
# Logging
# ==========================================
logging.level.com.elephant=INFO
logging.level.org.springframework.security=WARN

# ==========================================
# Application settings
# ==========================================
app.name=Elephant
app.admin.email=admin@elephant.com
app.admin.password=admin123
app.reminder.check-interval-ms=60000
